import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.szewczyk.flappypepe',
  appName: 'Flappy Pepe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
